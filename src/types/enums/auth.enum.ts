export enum CtUserRole {
  ADMIN = "admin",
  USER = "user",
  VIEWER = "viewer",
}

export enum CtUserStatus {
  PENDING = "pending",
  APPROVED = "approved",
  REJECTED = "rejected",
}

export enum CtAuthScreen {
  LOGIN = "Login",
  REGISTER = "Register",
  PENDING = "PendingApproval",
}
